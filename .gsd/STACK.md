# Technology Stack

> Auto-generated by /map on 2026-02-11

## Runtime

| Technology | Version | Purpose |
|------------|---------|---------|
| **Go** | 1.25.3 | Core runtime (Audio capture, client, networking) |
| **Python** | 3.x | Inference runtime (Server side) |

## Dependencies

### Production (Go)
| Package | Version | Purpose |
|---------|---------|---------|
| `github.com/gen2brain/malgo` | v0.11.24 | MiniAudio bindings for audio capture |
| `github.com/gorilla/websocket` | v1.5.3 | WebSocket client implementation |

### Production (Python - Inferred)
| Package | Version | Purpose |
|---------|---------|---------|
| `websockets` | Unknown | WebSocket server |
| `torch` | Unknown | PyTorch runtime for VAD |
| `numpy` | Unknown | Numerical operations |
| `silero-vad` | (hub) | Voice Activity Detection model |

## Infrastructure

| Service | Provider | Purpose |
|---------|----------|---------|
| **Localhost** | Self | IPC via WebSocket (port 8080) |

## Configuration

| Variable | Purpose | Location |
|----------|---------|----------|
| `port` | WebSocket Port (8080) | Hardcoded in `server.py` and `internal/network` |
| `sample_rate` | Audio Sampling (16kHz/44.1kHz) | Hardcoded in `server.py` |

## Outdated Packages & Risks

- **Missing `requirements.txt`**: Python dependencies are not pinned, leading to potential "works on my machine" issues.
- **Hardcoded Values**: Magic numbers for audio chunk sizes and sample rates scattered across `server.py` and Go code.
